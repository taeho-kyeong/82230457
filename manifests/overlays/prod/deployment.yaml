apiVersion : apps/v1
kind: Deployment
metadata:
  name: deploy-82230457 
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: deploy-82230457 
          command: ["java","-jar","/app.jar"]
          args: ["--spring.config.location=file:/config/application.yml"]
          volumeMounts:
          - mountPath: /mnt/cloud-sample
            name: cloud-sample-volume
            readOnly: false
            subPath: cloud-sample
          - mountPath: /config
            name: application-properties
            readOnly: true
                          
      volumes:
      - name: cloud-sample-volume
        persistentVolumeClaim:
          claimName: ce-82230457-sa-pvc
      - name: application-properties
        configMap:
          name: 82230457-configmap
          items:
            - key: application.yml
              path: application.yml
          defaultMode: 420
